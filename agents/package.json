{
  "name": "hero-mandate-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic demo runner for the Hero Mandate chain of mandate protocol",
  "scripts": {
    "selftest": "tsx src/selftest.ts",
    "typecheck": "tsc --noEmit",
    "run:sepolia": "tsx src/run.ts --chain sepolia",
    "run:robinhood": "tsx src/run.ts --chain robinhood"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
