// Agent track record: verifiable performance history from on-chain receipts.
// Placeholder while the page is under construction in this build pass.
export default function AgentsPage() {
  return (
    <main className="mx-auto max-w-[1280px] px-4 py-16 md:px-6">
      <h1 className="font-display text-2xl font-bold text-white">Agent track records</h1>
      <p className="mt-3 max-w-[560px] text-[15px] leading-relaxed text-muted">
        Verifiable agent resumes derived from on-chain receipts.
      </p>
    </main>
  );
}
